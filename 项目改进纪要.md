# 康识问诊 · 改进成果与 5 分钟汇报话术

> 面向：指导老师现场演示（约 5 分钟）
> 更新：2026-08-28（第四轮改进已完成，见「5.10」；第二、三轮汇总见「5.8 / 5.9」，明细见《改进清单.md》）

---

## 一、演示前检查清单

按顺序开，全部就绪约 2 分钟。**建议提前 10 分钟做完，并完整走一遍流程。**

| # | 动作 | 确认标志 |
|---|---|---|
| 1 | 启动 Docker Desktop（`%LOCALAPPDATA%\Programs\DockerDesktop\Docker Desktop.exe`） | 托盘图标不再转圈 |
| 2 | `docker start healthkb-redis healthkb-milvus` | `docker ps` 里 milvus 显示 `healthy` |
| 3 | 确认本地 MySQL 在跑（开机自启，端口 3306） | `netstat -ano \| findstr :3306` 有 LISTENING |
| 4 | 后端：`cd backend && mvn spring-boot:run` | 日志出现 `Started HealthKbApplication`，**且没有「不可用 / 改用内存」告警** |
| 5 | 前端：`cd frontend && npm run dev` | 打开 http://localhost:5173 |
| 6 | **预热一次问答**（很重要） | 随便问一句，确认能流式吐字 |
| 7 | **准备一张化验单照片**（报告解读演示用） | 手机拍一张自己的化验单，或用仓库 `samples/real-lab-cmp.jpg`（真实检验报告照片，Wikimedia Commons CC BY-SA 4.0） |

**演示账号**：`admin / Admin123!`（管理员，能看到「向量检索」页）；普通账号 `user / User123!`

> ⚠️ 第 6 步别省。提问要连外网调用 DeepSeek，网络慢的时候首次响应会卡几秒，提前跑一次能避免冷场。
> ⚠️ 第 4 步如果出现降级告警，说明 Redis 或 Milvus 没起来 —— 功能仍可用，但「三态一致」那段演示效果会变。

---

## 二、5 分钟脚本（「一个家庭的一次体检」故事线）

**要念的部分共约 920 字**，按 200 字/分约 4 分 35 秒；三处切浏览器演示各留 10–15 秒，合计约 5 分钟 —— 语速稍快一点就正好卡住。

引用块里是**可以照着念的完整句子**，不是提纲。核心叙事是「**体检完拿到化验单，看不懂，怎么办**」—— 拍照 → 解读 → 入档案 → 提醒 → 追着问，一步接一步，全部用真数据。讲得顺、还剩时间的话，从文末「§2.7 有余量再加」里挑一块补上。

| 段 | 时间 | 内容 |
|---|---|---|
| ① | 0:00–0:20 | 开场定位 |
| ② | 0:20–2:10 | 拍照化验单 → 解读 → 写入档案 → 首页提醒（两次切浏览器） |
| ③ | 2:10–2:40 | 急诊安全网 |
| ④ | 2:40–3:40 | 前端：报告单视觉 / 深色模式 / 工程质量 |
| ⑤ | 3:40–5:00 | 方法与收尾 |

### ① 开场 · 定位（0:00–0:20）

> 老师好。这个项目叫「康识问诊」，定位是一个**家庭健康助手**：老人体检完，化验单看不懂，指标偏了也不知道要不要紧。下面我按这个场景走一遍，全程真数据。

### ② 核心链路：拍照化验单 → 解读 → 档案 → 提醒（0:20–2:10）· 全场最有说服力

**👉 切到「报告解读」，上传一张化验单照片（提前准备好，见「一、演示前检查清单」第 7 步）**

> 这是最想展示的一步。这张图片**不经过传统 OCR**，而是直接交给多模态模型：它把「空腹血糖 7.2、收缩压 148」一行行读出来，自动对照参考范围判高低，逐项给解读。市面上很多系统只能处理文本报告，这一条做到了「拍下来就能用」。
>
> 解读完成，选一份档案写入。这里有个细节：同一份报告，上传时导入一次，之后再点「写入档案」不会写重——两条路径做了幂等比对。

**👉 切回首页**

> 一回到首页，异常指标已经变成一张「需要留心」卡片：血糖偏高、血压偏高，带参考范围，还标着「较上次」的变化点。点进去就是趋势曲线。

**👉 切到健康档案页，展示趋势，然后引出问答**

> 指标有档案了，问题就可以追着问。问答走的是 LangChain4j 的 RAG 流水线——知识库按 600 字切片、向量检索、增强生成，引用出处标在回答末尾；流式输出用的是 Spring AI 的 `ChatClient`。

### ③ 急诊安全网（2:10–2:40）

> 再补一条安全底线。模型回答之前，先用**确定性规则**拦一遍急症关键词——比如问「胸口剧痛」，120 提示会先于模型输出弹出来。在医学场景里，这种「宁可多说一句」的兜底，比再多两个花哨功能重要。

### ④ 前端：像产品 + 可维护（2:40–3:40）

**👉 演示右上角深色模式一键切换**

> 前端做了两件事。一是「像产品」：报告解读页按化验单的版式重排，异常行整行标色；流式回答带打字光标；深色模式一键切换，颜色全部走统一的设计 token。
>
> 二是可维护：问答页和健康档案页拆成子组件；工程侧后端 94 个测试全绿，覆盖越权、分页、幂等、限流；上传做文件头校验，问答限制长度，限流计数是 Redis 原子脚本。

### ⑤ 方法与收尾（3:40–5:00）

> 最后说方法。第一，把「引了依赖」和「真的在用」分开看——Spring AI、LangChain4j 最初只是 pom 里的两行依赖，现在才是真的在跑。第二，所有中间件都可缺省：没配大模型就回模板回答，Redis、Milvus 挂了有内存兜底，断网也能演示。
>
> 目前后端 94 个测试全部通过，前端类型检查和构建通过，MySQL、Redis、Milvus 都已接入。谢谢老师。

### §2.7 有余量再加（每块约 20–30 秒，按需插入）

**A. 第二个 bug —— 指标重复写入**（插在 ③ 后，约 30 秒；*注：该 bug 已修复，现在的脚本文案是「两条路径做了幂等比对」，这段作为「怎么发现的」即可*）

> 另一个是体检指标重复写入：上传报告时选了档案会导入一次，进详情页点「写入档案」又导一次，两条路径都没做幂等。这个不是读代码发现的 —— 是我造了真实数据跑完整流程，回数据库一查，16 条指标里有 4 条是重复的。

**B. 截图当验证手段**（插在 ⑤ 的两条方法中间，约 25 秒）

> 还有一条：改界面时用截图当验证手段。抓到过 SVG 注释里的语法错误导致整张图不渲染、心电图尾部拉平 —— 那在医学语境里是心跳停止。**这些构建和测试都不会报错。**

**C. Milvus 维度自愈**（插在 ② 技术要点之后或 ⑤ 降级段前，约 25 秒）

> 补一个细节：换成真 embedding 之后向量维度会变，而 Milvus 建表时维度是写死在 schema 里的。所以我加了自愈逻辑 —— 启动时比对已有集合的维度，不一致就自动删表重建，256 和 1024 两个方向都测过。

**D. 现场演示降级**（插在 ⑤ 的「中间件可缺省」处，约 30 秒，*需提前演练*）

> 我现场把 Milvus 容器停掉 —— `docker stop healthkb-milvus`，刷新这一页，状态变成「内存 / 未接入」，但问答依然能正常回答，因为内存里有完整备份。

> ⚠️ D 有风险：停掉后要重新 `docker start healthkb-milvus` 并等 healthcheck 变 healthy，而且后端要重启才会重新连上。**不熟练就别在老师面前做。**

---

## 三、一句话版本

> 这是一个家庭健康助手：化验单拍下来就能读、指标异常会自动提醒、问题可以追着问——底层是 Spring AI + LangChain4j + Milvus 的完整 RAG 链路，各层可缺省，断网也能演示。

---

## 四、可能被追问的问题

| 老师可能问 | 怎么答 |
|---|---|
| **为什么两个 AI 框架都要用？** | 分工不同：Spring AI 管对话层（流式输出、图片多模态），LangChain4j 管 RAG 流水线（切分 / 检索 / 增强）。各用所长，不重叠。 |
| **哈希向量和真 embedding 差在哪？** | 哈希只能匹配字面。「血压高了怎么办」和「高血压如何控制」是同一个意思，哈希命中不到同一篇文档，真 embedding 可以。现在真 embedding 为主、哈希兜底，所以没配 key 也能演示。 |
| **断网或者没有 Docker 还能演示吗？** | 能。四层各有降级路径且都实测过：大模型→模板回答，Redis→内存缓存，Milvus→内存向量库，embedding→哈希向量。 |
| **这些 bug 你是怎么发现的？** | schema 那个是通读建表脚本时看到顺序不对；指标重复写入是留痕测试造数据跑出来的，读代码看不出来。 |
| **Milvus 挂了检索还能用吗？** | 能，自动切内存余弦检索，88 条向量都在，只是重启后要重建索引。演示时可以现场停掉容器看。 |
| **改动量大概多少？** | 后端新增 6 个配置与适配类，重写了 LlmClient、RagService、TextChunker、CacheService 和整个 schema.sql；前端删掉一个页面、重构了样式体系。后续又做了：后端安全加固（请求校验/CORS/限流原子化）、四个列表接口分页、SSE 线程池有界化与 N+1 批查，测试从 20 个补到 90 个；第三轮做了拍照识别化验单（多模态）、首页异常指标总览、报告单视觉与深色模式、解读 PDF 导出；前端问答页与健康档案页各拆成 3–6 个子组件并接入 DOMPurify。 |
| **为什么选 Milvus，不直接用数据库？** | 关系库做向量相似度只能全表逐条算，数据量一上来就撑不住。Milvus 专门做近邻检索，建了 HNSW 索引后是次线性复杂度。这个项目数据量还小，选它主要是为了架构完整、可扩展。 |
| **为什么用余弦距离不用欧氏距离？** | 文本向量比的是语义方向而非绝对长度，余弦更合适，也是文本检索的通用做法。 |
| **Milvus 部署踩过什么坑？** | 最大的坑是拉镜像。Docker Hub 直连停滞十几分钟零进度，试了四五个国内镜像源大多没响应，最后只有一个能用。另外官方标准部署要额外起 etcd 和 MinIO，我改成 standalone 内嵌 etcd + 本地存储，三个镜像压成一个。 |
| **88 条向量是不是太少？** | 是演示数据量。来源是 WHO 等权威资料的切片，重点是把链路跑通；扩充只需往知识库导文档，会自动切分、向量化、写入。 |
| **为什么不用官方的 `sa-token-redis-jackson`？** | 它会拖进 Jackson 2，而 Spring Boot 4 用的是 Jackson 3，会冲突。只用了纯 DAO 的 `sa-token-redis-template`。 |

---

## 五、附：完整改进清单（备查，不用讲）

### 5.1 架构对齐 —— 清单上写了、代码里没有

| 项目 | 原状 | 现在 |
|---|---|---|
| **Spring AI 2.0** | pom 引了，全项目 0 处 import；手写 `HttpClient` 拼 OpenAI 请求 | `ChatClient` 流式问答，含图片多模态 |
| **LangChain4j** | pom 引了，0 处 import；RAG 完全自研 | 切分 / 检索 / 增强走官方流水线 |
| **JWT** | 配了 `token-style: jwt`（非法值，静默退化），发出去的是随机 UUID | 注册 `StpLogicJwtForSimple`，真三段式 JWT |
| **MySQL** | 默认数据源是 H2 | MySQL 为默认，H2 降为离线 profile |
| **向量检索** | 256 维哈希「伪向量」，无语义能力 | 真 embedding 为主，哈希兜底 |
| **Redis** | 只缓存问答结果 | 补会话上下文，后又补登录态 |

### 5.2 顺带查出的真缺陷（五项均已修复，复检确认；对应《改进清单.md》「阶段⓪」）

- **全新数据库缺字段（最严重）**：`schema.sql` 里 `ALTER TABLE` 排在 `CREATE TABLE` 之前，被 `continue-on-error` 吞掉。任何人第一次部署，`exam_report.profile_id` 根本不存在。现重写为纯幂等 `CREATE TABLE IF NOT EXISTS`。
- **SSE 问答在真实 Tomcat 上会崩**：Sa-Token 上下文过滤器只注册了 `REQUEST` 分发类型，流式回答结束后的 `ASYNC` 再分发拿不到上下文，直接抛异常。现显式注册全部 `DispatcherType`。
- **体检指标重复写入**：上传选档案导入一次，详情页「写入档案」再导一次，两条路径都无幂等。靠留痕测试才暴露。现 `importReportItems` 幂等比对（存量预载 + 循环内内存判定）。
- **模型名已下线**：DeepSeek 早已停用该模型名，老代码收到 400 后静默回退成模板答案 —— 表面「能用」，其实大模型一直没接上。现默认 `deepseek-v4-flash-vision-exp` 并同步配置文档。
- **SSE 连接泄漏**：客户端中断时未关闭上游流，连接会一直挂到超时。现服务端 try-with-resources 关闭上游订阅，前端 AbortController 主动断流（两端保障）。

### 5.3 前端：去模板感 + 建立秩序

- 删掉首页 01–04 四宫格、各页装饰性英文小标题、重复的导航分组；问答历史并入智能问答。
- 配图换成医疗主题 SVG（心电纸 / 监护仪 / 空折线图），**808 KB → 8 KB**。刻意避开「拉平的心电图」—— 医学语境里那是心跳停止。
- 圆角 8 种（12/14/16/18/20/22/24/28）收成 4 档 token。
- 表面分三级：**阴影只出现在最外层**，嵌套往深色走，不再层层叠白卡片。
- 新用户不再看到「一排零」：零值项不显示，且数字变成可点入口。

### 5.4 Milvus 向量库带来的变化

| | 接入前 | 接入后 |
|---|---|---|
| 检索方式 | 进程内逐条算余弦相似度 | HNSW 索引近邻检索（`M=8, efConstruction=64`） |
| 数据存活 | 重启即丢，每次都要重建 | 落盘持久化，重启后仍在 |
| 召回策略 | 直接取 top-k | 召回 24 条候选，再按医学词项精排截断（问答取 5 条，检索页取 8 条） |
| 集合 | — | `healthkb_chunks`，当前 88 条 / 256 维 / `MetricType.COSINE` |

**架构上的关键设计（值得讲）**

- **双写而非切换**：`RoutingVectorStore` 写入时**先写内存、再写 Milvus**，内存始终保有完整备份。所以 Milvus 不可用时检索依然准确，不是「降级成能用但不准」。
- **检索兜底**：优先查 Milvus；若 Milvus 返回空而内存有数据，自动回落内存。
- **维度自愈**：Milvus 建表时向量维度写死在 schema 里。换真 embedding 后维度会从 256 变 1024，因此启动时比对已有集合维度，不一致就自动删表重建（两个方向都测过）。
- **降级可见**：向量检索页直接显示当前后端、连接状态与降级原因，不用翻日志。

### 5.5 部署过程

**最终形态**

| 组件 | 部署方式 | 端口 |
|---|---|---|
| MySQL 8.0.41 | 本机已装服务（非容器） | 3306 |
| Redis 7 | Docker `healthkb-redis` | 6379 |
| Milvus 2.4.9 | Docker `healthkb-milvus`，standalone | 19530 / 9091 |

**两个部署决策**

1. **Milvus 只拉一个镜像**：官方标准 standalone 还要额外起 `quay.io/coreos/etcd` 和 `minio/minio`。改用内嵌 etcd（`ETCD_USE_EMBED=true`）+ 本地存储（`COMMON_STORAGETYPE=local`），三个镜像压成一个，国内拉取成功率大幅提高。
2. **MySQL 用本机服务**：容器镜像始终拉不下来（见下），而本机已装 MySQL 8.0.41，直接建库建用户即可。

**镜像拉取的实际经历**

- Docker Hub 直连：停滞约 13 分钟，层进度为零
- 试过 `docker.m.daocloud.io`、`dockerpull.org`、`docker.1panel.live`、`hub.rat.dev` —— 均无响应
- 只有 `docker.1ms.run` 可用，且只成功拉下 Redis 与 Milvus，MySQL 失败
- 用法：拉国内源后打回官方标签，compose 不用改

```bash
docker pull docker.1ms.run/milvusdb/milvus:v2.4.9
docker tag  docker.1ms.run/milvusdb/milvus:v2.4.9 milvusdb/milvus:v2.4.9
```

**健康检查**：Milvus 启动较慢，compose 里 `start_period` 设为 90 秒，探测 `http://localhost:9091/healthz`。实测冷启动约 30–60 秒变 `healthy`，**演示前务必等到这个状态**。

### 5.6 健壮性

- 会话落 Redis（`sa-token-redis-template`），登录态可跨后端重启。
- 但官方包把 Redis DAO 无条件注册（类上无任何 `@Conditional`），Redis 一停鉴权整体 500（已实测）。因此用 `@Primary` 抢下注入点，连不上就退回内存实现。
- 已知局限：该降级只在**启动时**判定一次，运行中途 Redis 掉线不会自动切回内存。

### 5.7 这些改动的实际意义

1. **技术栈从「写在 pom 里」变成「真的在跑」** —— 答辩时讲的每一项都能在代码里指出来。
2. **换一台机器能跑起来** —— schema 的洞会让任何全新部署第一次就出错。
3. **断网、没 Docker 也能演示** —— 四层降级逐条实测过，不是「理论上应该可以」。
4. **界面有了可复用的规则** —— 新页面照着 token 和三个类写即可，不会再一页一个样。

### 5.8 第二轮改进（2026-08-27，明细以《改进清单.md》为准；若被追问「后来又做了什么」按此答）

- **后端安全加固**：问答请求限长/限图（`@Size(max=500)` / `max=4` + `@Valid`）；CORS 源配置化；演示账号开关（`APP_SEED_DEMO_ACCOUNTS`）；文件名剥离 CR/LF 与控制字符；限流计数 Redis 侧改 Lua 原子化（消除 INCR+EXPIRE 之间崩溃导致的配额永久锁死）。
- **后端性能**：SSE 线程池有界化（8/32/16，满则 429「当前提问人数较多」）；Milvus flush 重试 8×1.5s → 3×400ms；Tika 截断上限提至 20 万字符并显式告警；收藏列表与历史消息两张 N+1 批查化；缓存清扫节流（<64 键跳过，≥64 每秒最多一次）；报告指标导入幂等预载。
- **列表分页（API 有变化）**：新增 `MybatisPlusConfig` 分页插件与统一 `PageResult<T>`；会话、消息、收藏、知识库四个列表接口改 `page/size`（size 上限 100）；前端同步「加载更多」「加载更早」与分页条；分页排序以 `id` 兜底保证同秒记录翻页不重不漏。
- **测试盲区补齐**：缓存、向量、切块、隐私越权、收藏、知识库管理、报告解析导入幂等、限流等新增覆盖，后端测试 20 → **87 个**；Mockito 按官方推荐以 `-javaagent` 运行，`mvn test` 在任意 JDK/CI 环境稳定。
- **前端清理与组织**：问答页 530 行、健康档案页 763 行拆为子组件 + composable；接入 DOMPurify 纵深防御；修复医学术语高亮改写 HTML 实体的 bug；词表加载、ECharts 注册、免责声明、flag 文案、Bearer 拼接各自收敛；登出复位 Pinia 内存态，admin 路由守卫先向服务端核验身份。
- **部署细节**：移除 Google Fonts CDN；vite 代理目标 env 化；移除零引用依赖；tsconfig 开启 `noUnusedLocals/noUnusedParameters`；删除 `*.vue` 通配类型 shim；限流窗口与上传限额 env 化。

### 5.9 第三轮：把「能不能用」变成「好用」（2026-08-27，即本文新脚本的演示内容）

- **拍照识别化验单**：图片报告不再要求贴文本——直接交给多模态模型转写（`LlmClient.extractReportText`），识别结果进原有「解析 → 解读 → 写入档案」链路；未配置多模态模型时降级为「写明原因的提示 + 粘贴文本/上传 PDF」兜底（原有 demo 文件名示例仍可用）。
- **真实报告可用性**：仓库示例图换成**真实检验报告照片** `samples/real-lab-cmp.jpg`（Wikimedia Commons "CMP report.JPG"，CC BY-SA 4.0）；解析器补齐英文指标名→中文映射、mg/dL→mmol/L·μmol/L 与 g/dL→g/L 单位换算、行号剥离、单位白名单——实测一张照片 11 项指标全部正确识别+判高低（血糖 9.1 偏高、尿素氮 9.6 偏高、谷丙转氨酶 47 偏高，其余正常）。
- **首页健康总览**：`/api/home/overview` 新增 `alerts`——按「档案＋指标类型」取最新异常指标（最多 6 条），带参考范围、档案名与「较上次」差值；首页渲染为「需要留心」卡片，点击直达档案。
- **前端视觉**：报告解读页改为**化验单版式**（异常行整行着色，窄屏自适应）；流式回答加**打字光标**；**深色模式**一键切换（`html.dark` + Element Plus 暗色变量，设计 token 全覆盖，ECharts 随主题重建配色）；首页卡片/空态统一。
- **报告 PDF 导出**：解读页「导出 PDF」在浏览器本地生成（html2canvas + jsPDF，A4 多页，无需后端渲染；实测 20MB→664KB 压缩后）。
- **测试**：新增 `ReportImageUploadTest`（未配模型降级提示、粘贴文本兜底）与 `HomeOverviewAlertTest`（告警聚合仅含异常项），后端测试 87 → **90 个**。
- **黑盒验证（本机 Chrome + Playwright，前后端已启动、真实模型）**：登录 → 首页 → 真流式问答（打字光标）→ 化验单照片识别 6/6 指标 → 报告单视觉与 LLM 解读 → 导出 PDF 落盘 → 深色模式切换 → 向量页（Milvus/88 条），**全程控制台 0 错误**；证据在 `.smoke-evidence/`。

### 5.10 第四轮：首页内容充实 + 导诊就医闭环（2026-08-28）

- **首页两栏改版 + 健康新闻**：主栏收窄、右侧新增可独立滚动的「健康新闻」流（公众号式图文卡：大图 + 标题 + 摘要 + 来源/日期）；`NewsCrawlerService` 在启动后与每 6 小时爬取**世界卫生组织中文新闻室**官方接口（列表 JSON → 正文页 `og:description`/正文段落 → cdn 配图下载到本地），按 `source_url` 去重、库留最新 30 条；断网自动落库 6 条内置科普快照（`seed-news.json`，内容取自 WHO 实况报告、来源指真实页面，配 6 张内置 SVG 插画）。点卡片进站内详情页 `/news/:id`（分段正文 + 来源外链 + 免责声明）。接口：`GET /api/news`、`/api/news/{id}`、`/api/news/{id}/image`（配图与聊天图片同套路，鉴权取 blob 不热链）。
- **导诊「附近医疗资源」**：导诊结果页底部新增区块——浏览器定位或手填地址（高德地理编码）→ 周边检索 3 公里内医院（优先综合/专科 `090101|090102`，不足退回大类 `090100`）与药房（精确类目 `090601`，避开大类的养生会所噪声）→ **真实 POI 喂给 LLM 只做解释**（提示词明令禁止编造列表外机构），卡片展示名称/距离/地址/可拨号电话。位置默认用完即走，勾选「保存此地址」才写 `user_location` 表（一人一条可清除）；无 key/断网降级为科室就医建议文字。接口：`POST /api/triage/nearby`、`GET/DELETE /api/triage/location`。
- **布局修复**：全局 `.page` 上限 1440px 贴左导致宽屏右侧一条死角白——改为水平居中，首页上限放宽 1760px；修掉 Chrome 下 `button` 上的 `display:grid` 不生效导致新闻卡塌陷的渲染 bug（grid 移入内层 span）。
- **报告详情页白屏修复**：vite 依赖预构建缓存缺 `jspdf`/`html2canvas`（仅报告详情页引用），懒加载 504 导致路由挂载失败整页空白；`vite --force` 重建预构建即恢复（生产构建不受影响）。排查线索：同路由不存在的 id 也白屏 + `/reports/upload` 正常 + 按版本参数直取 `/node_modules/.vite/deps/jspdf.js` 返 504。
- **留痕测试**：以 `user/User123!` GUI 走查全部功能并落真实数据（建档「爸爸 68 岁」、血压 148/95、文字问诊引用知识库、收藏、导诊内分泌科、图片问诊读真实化验单、报告上传 9 项解析）；新增可重放脚本 `scripts/smoke-image-ask-lab.py`、`scripts/smoke-report-upload.py`。前端 typecheck、生产构建、后端编译与 94/94 测试全部通过。

---

**当前状态**：后端 94/94 测试通过（2026-08-28 本机复跑确认），前端类型检查与构建通过；MySQL、Redis、Milvus 均已接入，启动无降级告警。4 个列表接口已改为统一分页结构（`records/total/page/size`，size 上限 100）。
