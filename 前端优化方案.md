# 前端优化方案（第五轮）

> 来源：2026-08-29 前端全量审计 + 逐页实测复核。
> 依据：通读 `frontend/src` 全部 55 个文件（约 9,900 行）；`vue-tsc --noEmit` 0 错误；
> `vite build` 23.4s 通过；把前后端跑起来（后端 h2 profile）并造带高低异常的演示数据后，
> 用 Chrome 无头逐页截图 18 张、在 14 个视口宽度上测量首页栅格、
> 对配色与可达性跑 `getComputedStyle` 断言。
> 与前四轮不同：本轮是**待办清单**，条目均为 `- [ ]`，做完勾选即可。
> 完整版（含活构件示例与截图证据）见两份在线文档：
> [前端审计](https://claude.ai/code/artifact/76a74635-01e9-4f0d-b075-1fe42aee07d8) ·
> [设计优化方向](https://claude.ai/code/artifact/cc83e3f9-6eaa-493b-af7e-dae1896960ee)

---

## 零、一句话结论

**这套设计的问题不在审美，在于主张只执行到了 token 层，没有贯穿到构件层。**

`styles/index.css` 开头那段自述（暖奶油底 / 深浓咖墨色 / 灰调鼠尾草绿 / Double-Bezel 同心圆角 / 胶片颗粒）
是一份真正有主张的设计说明，连「为什么避开陶土色」都论证过。但底下的构件是临时拼的：
色板很讲究，可 6 套 chip 各写各的；字体选得有见地，可 h3 有 9 种字号；
有一个极出色的签名控件（`LabStrip`），但它只出现在一个地方。

**核心主张：把「参考区间」变成整个产品的视觉语言。**
`LabStrip` 的注释里写着这个产品真正的洞察 ——「化验单的核心信息从来不是那个数字本身，
而是它落在参考区间的哪里」。下一步该做的，是让这句话统治整个界面。

---

## 一、工作量总览

去重后约 **20–24 小时**专注工作量（审计与设计两份文档有大量重叠，不能直接相加）。

| 包 | 内容 | 工时 | 风险 | 必要性 |
|---|---|---|---|---|
| A | 死类与语义色（6 个未定义类/变量 + 高低色说反） | 1.5h | 低 | **必做** |
| B | 演示会翻车的（断点死区、PDF 深色、固定列数） | 1h | 低 | **必做** |
| C | 系统三张表（间距 / 圆角 / 分类色板落 `index.css`） | 0.5h | 低 | **必做** |
| D | 构件收口（chip 六合一、删 `.slim`、h3 收四档、清硬编码色） | 1.5h | 低 | **必做** |
| E | 母题扩展（`Ruler` 三档 + 参考带 + 紧急度轨） | 3.5h | 中 | **必做** |
| F | 密度调整（首页快捷入口 / 空盒 / 等高 / margin 归位） | 1.5h | 低 | 推荐 |
| G | 组件化（`Shell` / `PageHeader` / `btn-cta` 推广） | 1.5h | 中 | 推荐 |
| H | 体积（EP 按需引入、PDF 库动态 import、KaTeX 移出首屏） | 2–3h | **高** | 可砍 |
| I | 无障碍组件层 | 1.5h | 低 | 推荐 |
| J | 逐页打磨（30 余条） | 4–6h | 低 | 长尾 |
| K | 验证（每档重跑 typecheck + build + 截图复核） | 1.5h | — | **必做** |

**真正的高价值核心是 A+B+C+D+E = 8 小时。** 改完这 8 小时，界面上肉眼可见的缺陷基本清零、
全站手感统一，且标尺母题铺开后信息传达方式有本质变化。剩下的收益递减。

> ⚠️ **H 是唯一有踩坑风险的**。Element Plus 按需引入会牵动三处容易漏的东西：
> `ElMessage`/`ElMessageBox` 这类命令式 API 的样式要单独引、
> 深色 token 依赖的 `theme-chalk/dark/css-vars.css` 不能删、
> `el-table`/`el-date-picker` 的子组件解析。真出问题可能从 2h 变 4h。
> **时间紧就整个砍掉** —— 它只影响加载速度，不影响演示时看到的任何东西。

**换算成小组进度**：需先读懂现有代码，按 2–3 倍算约 **40–70 小时**（一周多）。
建议分工：一人做 A+B+C+D（机械性改动，不需要设计判断），一人做 E+G（需要理解设计意图），
J 最后一起分页扫。

---

## 二、A 包 · 死类与语义色（1.5h，必做）

> 共同点是「写了但没生效」—— CSS 不报错，所以一直没被发现。全部已实测确认。

### A1. `--field` 变量从未定义

- [ ] `Vectors.vue:214` `:225` 的 `background: var(--field)` 改为 `var(--sunk)`

实测：`.ask input` 的 `backgroundColor` = `rgba(0, 0, 0, 0)`（完全透明）。
搜索框与背后的灰托盘融成一片，只剩一圈描边。

### A2. `.flag-high` / `.flag-low` 从未定义，异常值失去高低语义

- [ ] `AlertCenter.vue` 补 scoped 样式：

```css
.line-1 em.flag-high { color: var(--flag-high); }
.line-1 em.flag-low  { color: var(--flag-low); }
```

实测：三条异常提醒的数值实际颜色全是 `rgb(34,30,26)`（即 `--ink`）——
`86mmHg`（偏低）、`9.1mmol/L`（偏高）、`161mmHg`（偏高）三者**完全同色**。
而同一页面上方的 `LabStrip` 把同一个 86 画成蓝色、161 画成暖红。
**同一屏、同一份数据，两个组件给出了不同的颜色语言。**

### A3. `.rise` 用了 5 次，一次都没生效

- [ ] `Triage.vue` 5 处 `class="rise"` 换成已有的 `v-reveal` 指令，科室卡用 `v-reveal="i * 60"` 做交错

实测：遍历 `document.styleSheets` 统计命中 `.rise` 的规则数 = **`0`**。

### A4. `.shell` 单独使用时内容贴边（6 处）

- [ ] `Health.vue:95`、`ReportDetail.vue:53` `:59`、`Vectors.vue:31` `:43` `:47`
      内部补 `<div class="core core-pad">`

实测：这 6 处 `.shell` 的计算值为 `含 .core = false，padding: 6px，background: rgba(34,30,26,0.043)`。
截图里是平铺的灰色矩形、标题几乎贴着边框，紧挨着的正常卡片是白色纸面 —— 并排看像「样式没加载完」。

> 根治办法见 G 包：做成 `<Shell>` 组件，结构上就写不错。

### A5. 趋势圆点：偏低用了偏高的颜色

- [ ] `MetricTrendSection.vue:427` 拆开两条声明：

```css
/* 原：.trend-list .dot.high, .trend-list .dot.low { background: var(--flag-high); } */
.trend-list .dot.high { background: var(--flag-high); }
.trend-list .dot.low  { background: var(--flag-low); }
```

- [ ] 同文件 `.trend-list li.alert .note` 也写死了 `--flag-high`，一并按 flag 分色

实测：`class="dot low"` → `rgb(156,59,24)`，正是 `--flag-high` #9c3b18。
截图里「收缩压…最近一次**偏低**」配的是暖红圆点，而正上方的指标卡把同一个值画成蓝色。
**在医疗产品里颜色说反，不只是不好看。**

### A6. 「待观察」与「需复查」同色

- [ ] `AlertCenter.vue:196-212`：「需复查」用 `--flag-high` 全套（实底或强 wash），
      「待观察」降一级用 `--flag-none` 或中性描边胶囊

实测：两级严重度的**文字色完全一致**（均 `rgb(156,59,24)`），
背景又都淡到 8–10% 不透明度，截图上基本分不出来。严重度分级等于白做。

### A7. `.notice.low` 变体缺失，同一个「门诊」两种颜色

- [ ] `index.css` 补：

```css
.notice.low {
  background: var(--flag-low-wash);
  border-color: var(--flag-low-line);
  color: var(--flag-low);
}
```

实测：`.chip` 有 `.high/.low/.normal/.accent` 四个变体，`.notice` **唯独漏了 `.low`**。
导诊「门诊」级结果里，科室卡徽标是蓝色、顶部通告条却是中性灰，相隔 40px 两个颜色。

> A2 / A3 / A7 是同一类问题：**模板绑了类名，CSS 里没有对应规则，浏览器静默忽略**。
> 三条加起来说明这套系统缺一个「类名 ↔ 语义值」的收口 —— 见 C3。

---

## 三、B 包 · 演示会翻车的（1h，必做）

### B1. 1141–1280px 区间首页被挤到指标名截断

- [ ] `index.css` 把 `.bento` 断点从 900px 抬到 1280px，或改用容器查询：

```css
/* 方案 A：最省事 */
@media (max-width: 1280px) { .b4,.b5,.b6,.b7,.b8 { grid-column: span 12; } }

/* 方案 B：根治 —— 布局依赖的是容器宽度，不是视口宽度 */
.main-col { container-type: inline-size; }
@container (max-width: 560px) { .b4,.b5,.b6,.b7,.b8 { grid-column: span 12; } }
```

- [ ] `Home.vue` hero 的 `text-wrap: nowrap` + `max-width: 7.4em` 在窄栏会溢出，
      改 `max-width: min(7.4em, 100%)` 并允许换行

实测扫描（每个宽度真开一次浏览器量）：

| 视口 | 主栏 | 新闻栏 | b7 | b5 | b4（三张并排） |
|---|---|---|---|---|---|
| 1920 | 1266 | 336 | 733 | 519 | 413 |
| 1440 | 786 | 336 | 453 | 319 | 253 |
| 1280 | 626 | 336 | 359 | 253 | 199 |
| 1200 | 546 | 336 | 313 | 219 | 173 |
| **1141** | **487** | 336 | **278** | **195** | **153** |
| 1140 | 848 | — | 489 | 345 | 273 |
| 1024 | 732 | — | 421 | 297 | 235 |

1141px 截图上实际发生的：「最该看的一项」指标名被截成 **「空…」**；
底部卡片标题竖排成 **「读 / 过 / 的 / 报告」**；快捷入口最后一个图标被裁掉一半；
跨过 1140/1141 这条线卡片宽度从 153px 突跳到 273px。

> `@media (max-width: 1140px)` 是**含** 1140 的，所以挤压区在断点的**上方**（1141+），不是下方。

### B2. 深色主题下导出的 PDF 是白底白字

- [ ] `ReportDetail.vue:160` 导出前临时切浅色：

```ts
const wasDark = document.documentElement.classList.contains('dark')
if (wasDark) document.documentElement.classList.remove('dark')
await nextTick()
try { /* html2canvas ... */ } finally {
  if (wasDark) document.documentElement.classList.add('dark')
}
```

实测：深色下 `body` 的 `color: rgb(242,236,226)`（近白），
而 `html2canvas` 被硬编码 `backgroundColor: '#ffffff'`。且这是**静默失败** ——
不报错，`ElMessage.success('已导出 PDF')` 照常弹。

### B3. 导诊结果固定 3 列，只推荐 1 个科室时右边空 2/3

- [ ] `Triage.vue:389`（`.depts`）、`:573`（`.pois`）改 `repeat(auto-fit, minmax(240px, 1fr))`

全站其余四处（`Health.vue:496`、`Home.vue:748` `:769`、`ReportDetail.vue:257`）
用的都是 auto-fit，只有导诊页这两处是固定列。

---

## 四、C 包 · 系统三张表（0.5h，必做）

### C1. 间距：17 个 gap 值收成六档

- [ ] `index.css` 加：

```css
--space-1: 4px;  --space-2: 8px;   --space-3: 12px;
--space-4: 16px; --space-5: 24px;  --space-6: 32px;  --space-7: 48px;
```

现状统计：全站 17 个不同的 gap 值，含 3 / 5 / 7 / 9 / 11 / 15px 这些奇数。
不是精细调校，是每次写组件时临时决定的结果。

| 现状取值 | 出现次数 | 归入 |
|---|---|---|
| 2, 3, 4, 5 | 11 | space-1 (4px) |
| 6, 7, 8, 9 | 42 | space-2 (8px) |
| 10, 11, 12 | 46 | space-3 (12px) |
| 14, 15, 16 | 16 | space-4 (16px) |
| 18, 20 | 8 | space-5 (24px) |
| 26 | 2 | space-6 (32px) |

用法三级：图标与文字之间 `space-2`，卡片内元素之间 `space-3`，
区块之间 `space-5`，页面级分节 `space-6`。**档位少反而更容易做对。**

- [ ] 配套规矩：**间距归父级排版管，子组件不自带外边距**。
      删掉 `AlertCenter` 的 `margin-bottom: 16px` 与
      `MetricTrendSection` / `AdviceSection` / `HistorySection` 的 `.block { margin-top: 14px }`
      （现在档案页那一处间距是 36px，相邻区块都是 20px）

### C2. 圆角：补两档，清掉裸值

- [ ] 补 `--r-avatar: 10px`（头像方章，现 9/10/11px 混用）与 `--r-pill: 999px`（现 27 处写字面量）
- [ ] 组件里散落的 `7px` / `9px` / `10px` / `17px` / `22px` 全换成 token

现有五档（`--r-shell: 26px` / `--r-core: 20px` / `--r-card: 16px` / `--r-control: 11px` / `--r-chip: 8px`）
定义是好的，`--r-core = --r-shell − 6px 外壳内边距` 的同心计算尤其讲究 —— 别改坏。

### C3. 色彩：三层变四层，加四条规矩

- [ ] 新增**分类层**（图表专用，与语义层刻意不相交）：

```ts
// utils/charts.ts
// 原：['#40614b', '#9c3b18', '#2f5d7c', '#8a6d3b', '#6b7f5e']
//              ↑ --flag-high  ↑ --flag-low  ← 语义色被借走了
export const CHART_COLORS      = ['#40614b', '#7c8f6b', '#a89e93', '#5f6f7a', '#8f7f66']
export const CHART_COLORS_DARK = ['#8fb89b', '#a8b795', '#8b8175', '#8fa3b0', '#b3a184']
```

- [ ] `chartTheme()` 现在只切轴线与文字色，色板是写死的 —— 深色下 `#40614b` 几乎看不见，补一组暗色板

四条规矩（写进 `index.css` 注释）：

1. **语义层只能由数据的高低驱动。** 不是「这个字要醒目」就用 flag-high；
   反过来也成立：数据是偏低就必须是 flag-low，不能因为「偏低也是异常」而用暖色。
2. **分类层永不表意。** 图表按序列下标发色，色相与数值好坏无关，故必须与语义层完全不相交。
3. **品牌层不表示状态。** 鼠尾草绿是「这是康识」，不是「这项正常」；正常用 `--flag-normal`。
4. **任何一处颜色都必须是 token。** 组件里不出现十六进制或 rgba 字面量。

> 规矩 2 的现实后果：档案页趋势图里「空腹血糖」是第 2 条序列，
> 于是拿到 `#9c3b18` 被**画成红线** —— 但这份数据 5.2→5.5 全部在参考区间内，
> 同一页面上方还挂着绿色的「正常」徽标。读者看到红线的第一反应是「这项有问题」。

---

## 五、D 包 · 构件收口（1.5h，必做）

### D1. 清掉 6 处硬编码旧色板

- [ ] 逐处替换：

| 位置 | 写死的值 | 实际是 | 应替换为 |
|---|---|---|---|
| `AlertCenter.vue:202` | `rgba(220,38,38,.08)` | Tailwind red-600 | `--flag-high-wash` |
| `AlertCenter.vue:210` | `rgba(217,119,6,.1)` | Tailwind amber-600 | `--flag-none-wash` |
| `AlertCenter.vue:156` | `#fff` | 纯白 | `--on-accent` |
| `LabStrip.vue:206` | `rgba(15,23,42,.28)` | Tailwind slate-900 | 带纸面色相的阴影 |
| `Composer.vue:194` | `rgba(11,15,25,.62)` | 冷蓝黑 | `--ink` 系 |
| `ChatPhoto.vue:92` | `rgba(16,22,19,.78)` | 冷绿黑 | `--ink` 系 |

实测：深色主题截图里，「需复查」胶囊是整页唯一一处冷调饱和红色块，
在暖褐色深底上格外扎眼。而 `rgba(15,23,42)` / red-600 / amber-600
恰恰就是 `index.css` 开篇声明要避开的那组 AI 指纹里最典型的三个值。

### D2. 六套 chip 收成一套

- [ ] `index.css` 加 `.chip-btn`：

```css
.chip-btn {
  border: 1px solid var(--edge-strong);
  background: transparent;
  color: var(--ink-mute);
  border-radius: var(--r-pill);
  padding: var(--space-1) var(--space-3);
  font: inherit; font-size: 12.5px;
  cursor: pointer;
  transition: color .3s var(--ease-soft), border-color .3s var(--ease-soft),
              background .35s var(--ease), transform .35s var(--ease);
}
.chip-btn:active { transform: scale(0.96); }
@media (hover: hover) and (pointer: fine) {
  .chip-btn:hover {
    color: var(--accent);
    border-color: var(--accent-line);
    background: var(--accent-wash);
  }
}
```

- [ ] 替换 6 处局部实现：

| 实现 | 位置 | padding | 字号 | hover |
|---|---|---|---|---|
| `.seeds button` | Home | 5/12 | 12.5 | 有 |
| `.chips button` | MessageList | 7/14 | 13 | 有 |
| `.quick-chip` | Triage | 5/12 | 12.5 | 有 |
| `.qt` | MetricTrendSection | 5/14 | 13 | **无** |
| `.quick button` | CreateProfileDialog | 6/12 | 继承 | **无** |
| `.chips button` | Vectors | 5/12 | 继承 | **无** |

后三个没有 hover 态 —— **用户不知道它们能点**。

### D3. 删掉 4 份 `.slim`

- [ ] `DossierCard.vue:126`、`AdviceSection.vue:52`、`HistorySection.vue:79`、
      `MetricTrendSection.vue:463` 四处 `.slim` 全删，模板改用 `btn-sm`

`.slim { padding: 6px 12px; font-size: 13px }` 就是 `.btn-sm`，
只是少了 `gap` 和 `svg` 尺寸规则 —— 这些按钮一旦加图标，间距会和全站其他小按钮对不齐。

### D4. h3 收敛到四个角色

- [ ] 删掉 6 处组件内覆盖，按角色定义排版而不是按标签名

现状 9 种字号：

| 字号 | 出现位置 |
|---|---|
| 16px | `.section-head h3`（全局） |
| 17px | AlertCenter |
| 18px | Vectors、Triage `.dept h3` |
| 19px | `.empty h3`（全局）、Reports `.drop h3` |
| 20px | AdviceSection、HistorySection、MetricTrendSection、Knowledge |
| 21px | 全局 `h3` |
| 24px | DossierCard |

提案的四个角色：

| 角色 | 字体 | 规格 | 用于 |
|---|---|---|---|
| 展示 Display | Fraunces / 宋体族 | 500 · 38–62px · ls −0.024em | 仅 hero h1 与 section h2 |
| 标题 Title | Geist / 系统黑体 | 600 · 21px · ls −0.016em | 卡片主标题（人物名 24px 为唯一例外） |
| 区块标签 Section | Geist | 600 · 16.5px | 区块内分组标题（已存在于 `.section-head h3`） |
| 数据 Data | Geist Mono | 600 · tabular-nums · 26/20/15px | 化验值，**必须等宽** |

> 系统里其实已经埋了一个好判断：`.section-head h3` 把字体**换回了无衬线**，
> 说明作者意识到「衬线给大字，小字用衬线会糊」。只是没说出来、也没贯彻。

**一个中文特有的坑**：`fonts.css` 只声明了 Fraunces 的**拉丁子集**，
中文字形交给系统宋体 —— 这个取舍是对的，但后果是**中文大标题实际渲染的是系统宋体，不是 Fraunces**。
所以要避免同一个 h1 里中英混排（插一个 `BMI` 会突然变成对比强烈的 Fraunces，像贴上去的）。
另外 `letter-spacing: 0.2em` 对拉丁大写是标准做法，**对中文会把字距拉得很散**：

```css
.eyebrow { letter-spacing: 0.08em; }                      /* 中文默认 */
.eyebrow:lang(en), .eyebrow.latin { letter-spacing: 0.2em; }
```

---

## 六、E 包 · 母题扩展（3.5h，必做）

> **本轮收益最大的一档。** 改的是信息传达方式，不只是外观 ——
> 用户从「读数字做减法」变成「看位置」。

### E1. 抽出 `Ruler` 组件的三档形态

- [ ] 把 `LabStrip` 泛化成三种尺寸，语义与配色规则完全一致：

| 形态 | 规格 | 用于 |
|---|---|---|
| 全尺 Full | 26px 数值 + 轨道 + 刻度 + 徽标 | 指标卡、报告逐项（现状） |
| 紧凑 Compact | 20px 数值 + 轨道，无刻度文字 | 首页「其余越线的指标」（现状 `compact` prop） |
| **内联 Inline** | 84×4px 轨道 + 15px 数值 | **新增**：异常提醒行、趋势提示行 |

### E2. 铺到异常提醒行与趋势提示行

- [ ] `AlertCenter.vue` 的数值 + 「参考 90-139 mmHg」文字替换为内联标尺
- [ ] `MetricTrendSection.vue` 的 `.trend-list` 同理

现在异常提醒是纯文本「86mmHg 参考 90-139 mmHg」，要求读者自己心算「86 离 90 有多远」——
**这正是 `LabStrip` 被造出来要消灭的那种阅读负担**，却恰恰出现在最需要它的地方。

### E3. 趋势图加参考带

- [ ] `MetricTrendSection.vue` 给每条序列铺 `markArea`：

```ts
series: [{
  name, type: 'line', smooth: true, data,
  markArea: {
    silent: true,
    itemStyle: { color: 'rgba(74, 115, 85, 0.10)' },   // --flag-normal 极淡
    data: [[{ yAxis: band.low }, { yAxis: band.high }]],
  },
  markLine: {
    silent: true, symbol: 'none',
    lineStyle: { color: 'rgba(74, 115, 85, 0.4)', type: 'dashed', width: 1 },
    data: [{ yAxis: band.high }, { yAxis: band.low }],
  },
}]
```

参考区间从 `utils/metrics.ts` 的 `bandOf()` 取，与 `LabStrip` 同一数据源 —— **不新增判断口径**。
这一点很重要：这个产品里「什么算偏高」只能有一个权威来源。

加上参考带后，图表回答的问题从「这几个数字是多少」变成 **「我这些天在不在线内」**。

### E4. 趋势图双量纲拆轴

- [ ] 给第二组量纲配右侧 Y 轴（`yAxisIndex: 1`），或改成**每指标一张小图**纵向堆叠

实测：「我」的档案里有收缩压（84–89 mmHg）与空腹血糖（5.1–5.5 mmol/L），
Y 轴自动定到 0–100，**血糖被压成贴着 x 轴的一条完全水平的直线**，5.1 到 5.5 在图上是 0.4px。
首页的 `sparkOption` 已经是「一指标一图」的正确做法，档案页反而退回了合并轴。

### E5. 导诊紧急度换成三段轨

- [ ] 「急诊 / 门诊 / 可先自我观察」本质是一条有刻度的轴，
      用同一套轨道语言画成三段式 + 游标，紧急程度从「读一个词」变成「看一个位置」

顺带的结构收益：紧急度只有一个视觉出口，A7 那类「同一语义两种颜色」的分叉从结构上不存在了。

---

## 七、F 包 · 密度调整（1.5h，推荐）

> Editorial 的意思是**更少、更大、更笃定**，不是更多小卡片。
> 现在首页一屏内有 hero + 8 个快捷图标 + 7 个 bento 盒 + 新闻栏 = 17 个可点区域，注意力被摊平。

### F1. 快捷入口 8 → 4

- [ ] `Home.vue` 的 `QUICK` 数组砍到 4 个

现在 8 个里「问诊」「家庭档案」「科室导诊」「我的收藏」四个在左侧常驻栏已经有了 ——
同一个入口在一屏内出现两次。只留左栏**没有**的动作：读化验单、记指标、健康建议、病史。
这四个是「做一件事」，左栏那四个是「去一个地方」，区分开这一行才有存在理由。

顺带修掉：这一行是 `overflow-x: auto` 横滚条，1141px 下最后一个图标被裁掉一半且无滚动提示。

### F2. 两个空盒合并

- [ ] 「问过的」「读过的报告」合并成一个「最近」盒子，内部用小标题分组
- [ ] 两组都空时整个盒子不渲染 —— **空盒子不承载信息，只占版面**

（这条判断在 Health 页的指标卡上已经做对了，`filledCards` 只渲染有数据的，首页没跟上。）

bento 从 7 盒降到 5 盒：提问(b7) + 头条指标(b5) + 其余越线(b12) + 走势(b12) + 家人/最近(b6+b6)。

### F3. `.bento` 补 `align-items: start`

- [ ] `index.css` 的 `.bento` 加一行

实测：1440px 首页底部，「家人」卡有 3 个成员高约 290px，
旁边两张只有一行「还没有…」的卡被拉到同样 290px，各留约 200px 空白。

**同一个坑已经修过一次**：`Health.vue:498` 的 `.cards` 写着 `align-items: start`，
注释是「按内容定高：没数据的卡片不该被有数据的撑成同样高」；`Home.vue:750` 的 `.rest` 也设了。
唯独全局的 `.bento` 漏了。

### F4. hero 给足呼吸

- [ ] hero 到下一区块的间距拉到 `--space-7`(48px)，让那句论断单独成段

---

## 八、G 包 · 组件化（1.5h，推荐）

> 判断标准：**一个视觉模式出现三次以上，就该有名字。**

### G1. `Shell` 组件

- [ ] 新建 `components/Shell.vue`，把 A4 那 6 处手写两层 div 全换掉：

```vue
<template>
  <section class="shell">
    <div class="core" :class="pad !== false && 'core-pad'">
      <div v-if="$slots.head" class="section-head"><slot name="head" /></div>
      <slot />
    </div>
  </section>
</template>
```

### G2. `PageHeader` 统一

- [ ] 扩展 `PageHeader`（加 `#back` 插槽给报告页的返回链接），
      把 Health / Triage / Reports / Favorites / ReportDetail 5 个手写 `.head` 换过去
- [ ] 删掉 `icon` prop（两个调用点都传了但组件从不渲染）

现在两套写法的间距参数不同（手写 `margin: 4px 0 10px`，组件 `margin-bottom: 22px`），
页面之间标题到正文的垂直节奏对不齐。

### G3. `.btn-cta` 推广到各页面主行为

- [ ] 登录页「登录」、上传页「上传并解读」、导诊页「开始导诊」都换成 `.btn-cta`

Button-in-Button（箭头嵌在自己的圆壳里、hover 时斜向平移）是个很好的细节，
但目前**只用在首页 hero 一处**。**一个只出现一次的特殊形态是浪费，
出现在每个页面的关键动作上才叫语言。**

---

## 九、H 包 · 体积（2–3h，风险高，可砍）

真实构建产物（`vite build`，23.4s 通过）：

| 产物 | 原始 | gzip | 装的是什么 |
|---|---|---|---|
| `index.js` | **1,099 kB** | 367 kB | Vue + Element Plus 全量 + axios + pinia + router |
| `index.css` | **400 kB** | — | EP 完整 CSS 352K + KaTeX 24K + 设计系统 |
| `ReportDetail.js` | **598 kB** | 179 kB | jspdf + html2canvas，只为一个导出按钮 |
| `charts.js` | 530 kB | 179 kB | ECharts（已按需注册） |
| `markdown.js` | 382 kB | 135 kB | markdown-it + KaTeX + DOMPurify |
| KaTeX 字体 | 292 kB | — | 19 个 woff2 |
| dist 总计 | 4.6 MB | — | |

### H1. Element Plus 按需引入

- [ ] 装 `unplugin-vue-components`，`vite.config.ts` 加约 10 行：

```ts
import Components from 'unplugin-vue-components/vite'
import { ElementPlusResolver } from 'unplugin-vue-components/resolvers'
plugins: [vue(), Components({ resolvers: [ElementPlusResolver()] })]
```

- [ ] `main.ts` 删掉 `app.use(ElementPlus)` 与 `import 'element-plus/dist/index.css'`
- [ ] **保留** `theme-chalk/dark/css-vars.css`（深色 token 依赖它）
- [ ] `ElMessage` / `ElMessageBox` 这类命令式 API 需单独引样式

登录页只用到 `el-input` 一个组件，却要等 367 kB gzip 的 JS 解析完，
且 400 kB 的 `index.css` 是渲染阻塞的。保守估计能砍掉 index chunk 一半以上、CSS 的 3/4。

### H2. PDF 库动态导入

- [ ] `ReportDetail.vue` 把 `jspdf` / `html2canvas` 移进 `exportPdf()`：

```ts
const [{ jsPDF }, { default: html2canvas }] = await Promise.all([
  import('jspdf'), import('html2canvas'),
])
```

改完 `ReportDetail.js` 从 598 kB 降到约 15 kB。

### H3. KaTeX CSS 移出首屏

- [ ] `main.ts` 的 `import 'katex/dist/katex.min.css'` 移进 `utils/markdown.ts`（跟着 markdown chunk 走）
- [ ] 或更彻底：评估下来医疗科普问答里出现 LaTeX 公式的概率极低，
      整个摘掉 KaTeX、`applyMath` 一并删，能同时省掉 markdown chunk 一大半

---

## 十、I 包 · 无障碍组件层（1.5h，推荐）

> 系统层做得很好（`prefers-reduced-motion` / `reduced-transparency` / `contrast: more`
> 三个媒体查询、skip-link、`:focus-visible` 都有）。缺口全在组件层。

### I1. 异常提醒折叠头键盘打不开

- [ ] `AlertCenter.vue:3` 的 `<div class="head" @click>` 改成 `<button>`：

```vue
<button type="button" class="head" :aria-expanded="expanded"
        aria-controls="alert-list" @click="expanded = !expanded">
```

注意里面嵌了 `el-select` —— button 里不能嵌 button，需把筛选器移到 head 外面。

实测：实际属性 `{tag: "DIV", tabindex: null, role: null, ariaExpanded: null}`，四项全空。

### I2. 会话重命名/删除键盘不可达

- [ ] `SessionList.vue:35-36` 的 `<em>` 改成真 `<button>`；
      会话项本身改 `<div class="sess">`，内含三个按钮（打开 `flex:1` + 重命名 + 删除）

现在 `<em>` 不可聚焦，键盘用户**完全无法重命名或删除会话**（双击也不行）。

### I3. tab 只做了一半 ARIA

- [ ] `Health.vue:44-58` 补 `aria-controls`、对应的 `role="tabpanel"`、左右方向键切换

实测：`[role="tab"]` 的 `aria-controls` = `null`，全页 `[role="tabpanel"]` 数量 = `0`。
**半套 ARIA 有时比没有更糟 —— 它给了承诺却没兑现。**

### I4. 图标缺 `aria-hidden`

- [ ] `icons.ts` 里 31 个 `<svg` 统一加 `aria-hidden="true" focusable="false"`（一次改完）

读屏会遍历 SVG 内部图形；在「快捷入口」这种一行 8 个图标按钮的地方，
每个按钮的可读名称前都会多出一段图形噪音。

### I5. 流式回答没有 `aria-live`

- [ ] `onDone` 后把完整回答放进一个视觉隐藏的 `aria-live="polite"` 容器播报一次，
      流式过程中只播报「正在回答」（逐 token 播报会很吵）

---

## 十一、J 包 · 逐页打磨（4–6h，长尾）

> 没有一条是「坏了」，都是打磨。建议**按页推进**而不是按类型 ——
> 一次改完一整页，收益能立刻看见，也便于对照截图验收。

### 登录 / 注册 `Login.vue`

全站完成度最高的一页。

- [ ] 主按钮换 `.btn-cta`（全站唯一一处「主行为却没有 CTA 形态」）
- [ ] 左栏三条卖点的图标砖改 `--accent-wash` 底 + `--accent-line` 描边
      （现在 `--card` 底在 `--sunk` 左栏上对比很弱）
- [ ] 注册的密码规则（≥6 位）改成密码框下方常驻小字 ——
      **约束应该在输入前告知，而不是在失败后弹 toast**
- [ ] `.link:hover` 的 `text-decoration: underline` 去掉，全站链接都是变色不加下划线

### 今天（首页）`Home.vue`

- [ ] 「最该看的一项」指标名允许两行（`-webkit-line-clamp: 2`）而不是单行省略 ——
      **指标名截断等于这张卡失去意义**
- [ ] `NewsPhoto` 的图片处理是**做对了的**（`aspect-ratio: 16/9` + 占位底 + 淡入 + revoke，一处不缺）。
      唯一可优化的是加载策略：首页一次渲染 12 张卡各发一个鉴权 blob 请求 = **12 个并发**。
      建议首屏可见的 3–4 张立即加载，其余用 `IntersectionObserver` 进视口再取
      （`loading="lazy"` 对 `fetch` 拿的 blob 不起作用）
- [ ] 新闻栏 `max-height: calc(100dvh - 152px)` 的 152 是手算魔数，
      提成 `--topbar-h` / `--main-pad` 两个 token 与 `AppLayout` 共用
- [ ] `Home.vue:163` 的 `:class="'b4'"` 改成 `class="shell b4"`（静态字符串写成了动态绑定）

### 问诊 `Chat.vue` / `MessageList` / `Composer`

空态写得很好 ——「把症状、药品或检查，说具体一点」是邀请而不是「暂无数据」。

- [ ] `.answer` 加 `max-width: 34em`（现在 `max-width: none`，宽栏下一行能排到 60 字以上）
- [ ] 「收藏该回答」改成常驻但用 `--ink-faint` 弱化，hover 提升到 `--ink-soft`
      （现在 `opacity: 0`，键盘 Tab 到时会突然跳出来）
- [ ] 流式光标的字符 `▍`(U+2589) 换成 `2px × 1em` 的 `::after` 伪元素
      （不同系统字形宽度不一，导致文字末尾抖动）
- [ ] 窄屏会话列表改抽屉（复用 `AppLayout` 已有的 rail 抽屉模式），
      现在压成 152px 矮条与消息区形成两个叠着的滚动容器
- [ ] Composer 的 `.hint`「化验单、药盒、患处照片都能发」窄屏被 `display: none` ——
      这句话恰恰是移动端最需要的引导，改成移进 placeholder 或缩短保留
- [ ] 修 blob URL 泄漏：`Composer.vue:113` 的 `submit()` 清空 `pending` 前先 revoke；
      `useChatStream.ts:41` 的 `localPreviews` 从不 revoke（每发一张图漏两个）

### 档案 `Health.vue` + 六个子组件

信息量最大，也是构件不一致最集中的一页。

- [ ] 「看趋势 / 标为已处理」的 `el-button text` 换成 `.btn-quiet.btn-sm`
- [ ] MemberBar 的「本人 · 年龄未填」改成「本人」——
      **用否定句描述空值会让人觉得自己做错了什么**；真要引导补全用可点的弱链接
- [ ] MemberBar 的文本「+」换成 `ICONS.plus`（首页同一语义用的是 SVG，粗细对不上）
- [ ] 指标表格 7 列（类型/数值/单位/高低/时间/备注/删除）合并成三列：
      **指标（含单位）· 数值（带内联标尺）· 时间**，删除放 hover 显形
- [ ] 四个页签的计数统一显示，0 时用 `--ink-faint`（现在 `v-if` 让 0 不显示，但 0 本身是有用信息）

### 报告解读 / 详情 `Reports.vue` / `ReportDetail.vue`

上传页的投放区（虚线 → 实线 + 绿底「已就绪」）是很好的状态设计。

- [ ] 详情页改成单栏窄版（`max-width: 720px`）、白纸底、逐项之间用发丝线而非卡片间隙 ——
      **让它读起来像一份可以打印的报告**，正好与 PDF 导出目标一致
- [ ] 「原文摘录」的 `<pre>` 换成 `--body` 字体 + `white-space: pre-wrap`
      （等宽字体的 CJK 回退不受控，中文字距会很怪）
- [ ] 上传的「解读中…」加分步提示（识别 → 提取指标 → 解读）——
      **十几秒的静默会让人以为卡住了**

### 科室导诊 `Triage.vue`

- [ ] 匹配分现在裸显示一个「5」，无单位无量纲说明。
      要么去掉数字只留进度条，要么改「匹配度 高/中/低」——
      **一个读者无法解释的数字不如不给**
- [ ] 电话的 `☎`(U+260E) 换成 `ICONS` 里新增的 phone 图标
      （macOS 可能渲染成彩色 emoji，Windows 是黑色字形）
- [ ] 「导诊说明」正文已含免责声明，页面底部 `MedicalDisclaimer` 又出现一次 ——
      同一屏两条会稀释分量，去掉其一

### 管理端 `Vectors.vue` / `admin/Knowledge.vue`

> 实测下来管理端视觉**并没有脱节** —— EP 的 token 覆盖是生效的（主色鼠尾草绿、
> 暖奶油圆角卡、表头吃到 `--sunk`、删除按钮是 `#9c3b18`）。问题在别处。

- [ ] 向量页检索前是一大片空白，设计系统里有现成的 `.empty` 构件没用上。
      补一个：「输入症状或药名，看看知识库会召回什么」
- [ ] Milvus 的 gRPC 原始报错（`DEADLINE_EXCEEDED` / `buffered_nanos` / `wait_for_ready`）
      直接铺了两行 —— 首句常驻、余下收进「详情」折叠。
      **现在的样子会让演示观众以为系统坏了，而实际上降级逻辑工作得很好**
- [ ] 知识库时间列走 `formatWhen()`（现在是原始纳秒时间戳 `2026-08-29T14:35:33.517145`）
- [ ] 「原文」列铺开整条 URL 折成三行、行高撑到 60px+，
      改成域名 + 「查看原文 ↗」，完整 URL 放 `title`
- [ ] 后台表单的 `label-width="80px"` 右对齐标签统一到前台的「标签在上」写法

### 全局骨架 `AppLayout.vue`

- [ ] 左栏底部引导卡在上传报告页自己也显示，加 `v-if`；
      更好的做法是随上下文换内容（档案页提示「记一条血压」，问诊页提示「可以直接发图」）
- [ ] topbar 58px 高，右侧大片空置。让它承载页面级动作
      （档案页的「新建档案」、知识库的「同步」），从装饰条变成工具条
- [ ] 面包屑现在是 `route.meta.title` 纯文本无层级。
      二级页面应显示「档案 / 报告详情」并让前一级可点 ——
      现在这些页面各自在正文里手写了「返回档案」，是同一件事做了两遍
- [ ] 主题切换做成三态（浅 / 深 / 跟随系统），默认仍是浅色
      （`initTheme()` 不跟随 `prefers-color-scheme` 的决定可以保留）

---

## 十二、动效三条规矩（并入各档，无独立工时）

> 现有实现质量是高的 —— 全部自定义 cubic-bezier、`v-reveal` 走 IntersectionObserver
> 而不是 scroll 事件、`prefers-reduced-motion` 完整兼顾。问题只出在**用在哪里**。

1. **首屏不做入场动效。** `.reveal` 的 `filter: blur(6px)` + 0.8s 意味着
   每次进首页 hero 都要模糊 0.8 秒才清晰 —— 感知性能的代价很实在，收益是零
   （用户还没滚动，没有「揭示」这回事）。把 `v-reveal` 限制在折叠线以下。
2. **三档时长，按动的是什么来定。** 状态变化（颜色/透明度）≤ 200ms；
   位置/尺寸变化 ≤ 400ms；入场揭示 ≤ 600ms。现在有 0.15s 到 0.8s 共 9 种时长散落各处。
3. **数据驱动的动效多用，装饰性的少用。** `LabStrip` 游标的 `transition: left 0.5s`
   是全站最好的一个动效 —— 数值更新时游标滑过去，动作本身传达了「变化了多少」。
   而卡片 hover 的 `translateY(-3px)` 在密集列表里会让整页跳动。

**一个技术细节**：`.tile:hover` 同时动 `transform` 和 `box-shadow`，
后者每帧触发重绘。首页新闻栏一次渲染 12 张卡，滚动时若有 hover 会明显掉帧：

```css
.tile { position: relative; }
.tile::after {
  content: ''; position: absolute; inset: 0;
  border-radius: inherit; box-shadow: var(--shadow-3);
  opacity: 0; transition: opacity .45s var(--ease); pointer-events: none;
}
.tile:hover::after { opacity: 1; }   /* 只动 opacity，不触发重绘 */
```

---

## 十三、残留清理（并入 D 档）

- [ ] `utils/format.ts` 的 `greeting()` 与 `clip()` 全项目零引用，删除
      （`greeting()` 是首页早期「早上好」问候语的遗留；
      改成「N 项指标越过了参考线」是明显更好的决定，函数忘了删）
- [ ] `icons.ts` 里 `more` / `user` / `pill` 三个图标零引用，删除
- [ ] `README.md:11` 的设计系统描述照实改写 —— 现在写的是
      「**Apple 风格设计系统（毛玻璃材质分层、弹簧曲线动效、系统字体栈）**」，
      而实际是「**Editorial Luxury：暖奶油底 / 深浓咖墨色 / 灰调鼠尾草绿 /
      高对比可变衬线大标题 / CSS 胶片颗粒**」，字体是下载的 Fraunces + Geist 而非系统字体栈。
      **实际那套比 README 里那句泛泛的描述有说服力得多，等于把最亮的一张牌盖住了。**
      建议把 `index.css` 开头的设计纪律摘几条进 README

---

## 十四、验证（K 包，1.5h，必做）

每完成一档执行：

- [ ] `cd frontend && npx vue-tsc --noEmit`（应为 0 错误）
- [ ] `npx vite build`（应通过；H 档改完对比产物尺寸）
- [ ] 逐页截图复核。可复用本轮的脚本思路：
      `puppeteer-core` 驱动本机 Chrome，注入 token 免登录，
      对关键点跑 `getComputedStyle` 断言而不是只看截图
- [ ] 重点回归：1141px / 1440px 两个宽度的首页；深浅两套主题的档案页；
      导诊结果页（1 个科室与 3 个科室两种情况）

> 本轮实测出的教训：**静态读代码能定位问题，但定不准边界。**
> 第 B1 条我最初把挤压区间判断成 900–1140px，实测才发现是 1141–1280px（断点的另一侧）；
> 管理端「视觉脱节」的判断也在截图后被推翻。**改完务必截图复核，不要只信推理。**

---

## 十五、做完能拿到什么

| 维度 | 现在 | A–E 做完后 |
|---|---|---|
| 肉眼可见的缺陷 | 6 个未定义类/变量、2 处语义色说反、1 个断点死区 | 清零 |
| 构件一致性 | 6 套 chip、4 份 `.slim`、9 种 h3 字号、6 处硬编码色 | 各 1 套 |
| 信息传达 | 异常提醒要心算「86 离 90 有多远」 | 内联标尺，看位置即可 |
| 图表 | 正常指标被画成红线；血糖压成水平直线 | 分类色板 + 参考带 + 拆轴 |
| 设计语言 | 主张停在 token 层 | 贯穿到构件层 |

**判断这一轮做成没做成的标准只有一条：**
把首页、档案页、导诊页并排放在一起，看起来像不像同一个人做的。
