# 改进清单

> 来源：2026-08-27 全量代码审查（后端 6 类扫描 + 前端 6 类扫描）。
> 第一轮四阶段已完成（见下「第一轮」），第二轮安全/性能/分页/组织也已完成（见「第二轮」），
> 第三轮体验升级（拍照识别化验单/首页总览/视觉与深色模式/PDF 导出）也已完成（见「第三轮」）。
> 后备池只剩未排期事项。
> 2026-08-27 复检更新：清单各 [x] 项已逐项与代码核对；本轮补齐两处 ——
> 「阶段⓪」回填前轮已修复的 5 个真缺陷（原只记在《项目改进纪要.md》5.2，本清单漏记），
> 并在第二轮 C/D 追加「分页排序确定性」修正与 Mockito agent 配置（复检时发现的真实问题）。

---

## 二轮执行总览（2026-08-27 完成，后端 87/87 测试通过、前端 vue-tsc + build 通过、浏览器冒烟通过）

### A. 后端安全加固 ✅

- [x] **AskRequest 校验** — `question @Size(max=500)`、`imageIds @Size(max=4)`，`ChatController.ask` 补 `@Valid`
      （超长文本不再直通 embedding 与 LLM）
- [x] **CORS 配置化** — `app.cors.allowed-origins`（env `APP_CORS_ALLOWED_ORIGINS`，逗号分隔多值）
- [x] **种子账号开关** — `app.seed-demo-accounts`（env `APP_SEED_DEMO_ACCOUNTS`，默认 true）
- [x] **Content-Disposition 加固** — 文件名剥离 CR/LF 与控制字符
- [x] **RateLimiter Redis 原子化** — INCR+EXPIRE 合并为 Lua 脚本执行，
      消除两命令间崩溃导致 key 永不过期、配额永久锁死的窗口

### B. 后端性能 ✅

- [x] SSE 线程池有界化：cachedThreadPool → core 8 / max 32 / 队列 16，超出抛 429「当前提问人数较多」
- [x] Milvus flush 重试：8 次×1.5s 递增（最坏 54s）→ 3 次×400ms
- [x] Tika 截断显式化：上限提到 20 万字符并在触顶时 log.warn（原为默认 10 万静默截断）
- [x] FavoriteService.list N+1 → messageId 批查
- [x] ChatService.loadHistory N+1 → 附件图片按 messageId in 查询
- [x] CacheService.evictExpired 节流：<64 键跳过，≥64 时每秒最多全表清扫一次
- [x] HealthService.importReportItems：幂等比对存量一次预载 + 循环内内存判定

### C. 列表分页 ✅（API 有变更）

- [x] 新增 `MybatisPlusConfig`（注册 PaginationInnerInterceptor；3.5.9 起需另引 `mybatis-plus-jsqlparser` 依赖）
      与统一返回结构 `dto/PageResult<T>`（records/total/page/size，size 钳制 ≤100）
- [x] 四个接口从返回数组改为 PageResult：
      `GET /api/chat/sessions?page&size`（默认 50）、`GET /api/chat/sessions/{id}/messages?page&size`
      （默认 50，第 1 页=最新）、`GET /api/favorites?page&size`（默认 20）、
      `GET /api/admin/knowledge?page&size`（默认 20）
- [x] 前端同步：chat/favorites/knowledge API 与类型、Chat 会话侧栏「加载更多」+
      消息区「加载更早」prepend、Favorites 与 admin Knowledge 接 el-pagination
      （删当前页最后一条自动回退一页防空页）
- [x] **分页排序确定性**（复检补）— `created_at`/`updated_at` 均为秒级 TIMESTAMP，
      同秒多条时「ORDER BY 时间 DESC」并列顺序不稳定，LIMIT/OFFSET 翻页会重复/漏行。
      四个分页接口在时间排序后追加 `id DESC` 兜底：
      `listSessions`（updated_at, id）、`listMessages`（created_at, id）、
      `FavoriteService.list`（created_at, id）、`KnowledgeService.listDocuments`（created_at, id）；
      并修正 `ChatApiPrivacyTest.messagePaginationReturnsNewestFirstInSmallPages`
      第 2 页页首断言（6 条消息/每页 2 条时第 2 页 = 第二轮问答，原断言按 4 条假设写错）

### D. 测试盲区补齐 ✅（累计 87 个测试）

- [x] `CacheServiceTest` — set/get、TTL 过期、evict、覆盖写（4 例）
- [x] `EmbeddingServiceTest` — 确定性、维度、区分度、L2 归一化、normalize（6 例）
- [x] `TextChunkerTest` — 真实 recursive(600,80) 切块、CRLF 归一、多块切分（4 例）
- [x] `ChatApiPrivacyTest` — 跨用户读/删会话 404、级联删除、消息分页语义（4 例）
- [x] `FavoriteApiTest` — 重复收藏幂等同 id、越权删除 404、他人列表不可见、分页字段（4 例）
- [x] `KnowledgeAdminApiTest` — 录入→列表可见→检索召回→删除不召回全链路 + 非 admin 403（2 例）
- [x] 共用测试基建 `support/ApiTestClient`（独立注册用户 + 提问收流）
- [x] **Mockito agent 配置**（复检补）— 内联 mockmaker 默认运行时自附加 agent：
      JDK 21+ 已告警、更高版本将直接失败，受限环境（如 CI 沙箱）里自附加会找不到外部进程，
      `RebuildDecisionTest`/`RoutingVectorStoreTest` 报「Could not initialize MockMaker」。
      pom 增加 surefire `argLine: -javaagent:…/mockito-core-${mockito.version}.jar`（Mockito 官方推荐），
      `mvn test` 在任意 JDK/环境稳定跑绿。
- [x] `ReportImportIdempotencyTest` — 同一报告「上传导入 + 详情页写入档案」两次只留一条（1 例，
      复检补：阶段⓪「指标重复写入」修复此前无自动化用例）

### E. 前端清理与组织 ✅

- [x] 死代码删除：`components/AuthImage.vue` 整删；markdown.ts 的 `questionTerms`/
      `citationFitsQuestion`/`CITE_STOP` 删
- [x] DOMPurify 接入 renderMarkdown（html:false 的纵深防御）
- [x] 医学术语高亮修复：术语替换先按 HTML 实体切开只在非实体片段内做，
      且合并为单轮 alternation 替换（修掉 `&lt;` 被改写成 `&<span>lt</span>;` 与
      多轮替换把上一轮 span 标签卷进匹配两个缺陷）
- [x] `utils/authedFetch.ts` 统一 Bearer 拼接（sse.ts、ChatPhoto.vue 收口）
- [x] `composables/useTerms.ts` 收敛 4 处复制的词表加载
- [x] `utils/charts.ts` 集中 ECharts 注册 + CHART_COLORS（Health/Vectors 复制消除）
- [x] `components/MedicalDisclaimer.vue` 免责声明组件化（Home/Triage/Reports/ReportDetail + AdviceSection 内）
- [x] flagText 双实现收敛到 utils/metrics（unknown 统一译「未知」）
- [x] Home.vue 两处原始时间戳改走 formatWhen
- [x] **Chat.vue 530 行拆分**：`components/chat/{SessionList,MessageList,Composer}.vue` +
      `composables/useChatStream.ts`；主视图只剩编排与分页逻辑
- [x] **Health.vue 763 行拆分**：`components/health/{MemberBar,DossierCard,MetricTrendSection,
      HistorySection,AdviceSection,CreateProfileDialog}.vue`；主视图只留数据编排
- [x] 登出复位 Pinia 内存态（clearSessionAndRedirect 动态 import 防循环依赖）；
      admin 路由守卫在内存无 user 时先 fetchMe 校验真实身份再放行

### F. 部署细节 ✅

- [x] index.html 移除 Google Fonts CDN（字体栈保留 Noto 名称 + 系统字体回退）
- [x] vite proxy target 支持 `VITE_PROXY_TARGET` env（timeout:0 注明是 SSE 有意为之）
- [x] 移除零引用依赖 `@element-plus/icons-vue`
- [x] tsconfig 开启 noUnusedLocals/noUnusedParameters（暴露并清掉 3 处死 import）
- [x] env.d.ts 删 `*.vue` 通配 shim（vue-tsc 下它会弱化全部 SFC 类型）
- [x] 限流窗口 env 化：`APP_RATE_LIMIT_WINDOW_SECONDS` 应用于全部四项
- [x] 上传限额 env 化：`APP_UPLOAD_MAX_BYTES` / `APP_UPLOAD_MAX_REQUEST_BYTES`

### G. 验证 ✅

- [x] 后端 `mvn test` 87/87 全绿；前端 `vue-tsc --noEmit`（含 strict unused 检查）与
      `npm run build` 通过
- [x] 浏览器冒烟（h2 profile 后端 + dev 前端，GUI 黑盒）：登录 → 首页（时间戳/免责声明组件）→
      问答页拆分后提问/流式/停止按钮状态切换/收藏按钮出现 → 收藏页带内容渲染 →
      健康档案页六个子组件渲染 + 新增指标弹窗全链路 → 知识库管理页分页接口渲染。
      无控制台可见错误、无布局破损。

---

## 第三轮 体验升级（2026-08-27，后端 90/90 测试、前端 vue-tsc + build 通过）

> 目标：把「功能齐」变成「好用、有亮点」。核心叙事 = 一个家庭的一次体检：
> 拍照化验单 → 多模态识别 → 解读 → 写入档案 → 首页「需要留心」→ 趋势 → 追着问 → 急症拦截。

### H. 拍照识别化验单（报告的图片不再需要手贴文本）✅

- [x] `LlmClient.extractReportText`（新增）—— 图片字节 → 多模态模型转写为逐行文本；
      复用既有 `ChatClient` + `Media` 通道，不引入新依赖；未配置模型/模型无视觉/识别为空均抛带成因的异常
- [x] `ReportService.upload` 图片路径：Tika 文本层为空且非 demo 文件名 → 走 `extractReportText`；
      失败降级为明确 400 提示（配置 `APP_LLM_API_KEY` 或粘贴文本/上传 PDF/Word/txt），demo 文件名示例保留
- [x] `ReportImageUploadTest` — 未配置模型时的降级文案 + 附带 extractedText 的图片照常解析（2 例）
- [x] **真实化验单可解析**（复用真实照片实测发现并修复）：`MetricGuide.normalize` 英文/缩写指标名 → 中文标准名
      （GLUCOSE→空腹血糖、[BUN]→尿素氮、ALT(SGPT)→谷丙转氨酶、CREATININE, SERUM→肌酐 等 40+ 别名），
      行首检验编号剥离、括号/逗号注解清洗；`ReportParser` 增加 **mg/dL↔mmol/L / μmol/L、g/dL→g/L 单位换算**
      （血糖÷18.02、胆固醇÷38.67、甘油三酯÷88.57、尿素氮÷2.8、肌酐×88.4、胆红素×17.1、白蛋白/总蛋白/血红蛋白×10，
      按数值量级自动识别单位制）、HI/LO 高低标志剔除、**单位正则白名单**（防两列式 OCR 把下一段英文名错位进单位位）、
      中文参考范围补全（谷丙/谷草转氨酶、尿素氮、肌酐 57-104、钙钾钠氯、白蛋白、总蛋白、总胆红素、碱磷酶、肌酸激酶）；
      `HealthService.importReportItems` 导入前同样归一 —— **英文报告能看懂、能判高低、能写入档案**
- [x] `ReportParserTest` 新增英文报告用例（别名+血糖/胆固醇换算、60 mg/dL 偏低仍判 low、[BUN]/ALT(SGPT) 正名、
      逗号名称+白蛋白 g/dL 换算、肌酐/尿素氮/胆红素 mg/dL 换算判定）(+3 例，累计 94)

### I. 首页健康总览仪表盘 ✅

- [x] `HomeDtos.Overview` 新增 `alerts`：按「档案+指标类型」取最新异常指标（高/低，最多 6 条），
      带参考范围、档案名、与同类型上一条的差值（delta）；正常值不入列
- [x] `HomeService.latestAlerts` 实现（一次扫描 600 条上限内内存聚合，无逐条 N+1）；`HomeOverviewAlertTest`（1 例）
- [x] Home.vue 重构：hero 下方新增「需要留心」卡片组（flag 左色条、参考范围、较上次差值，点击直达档案）；
      无异常时显示绿色「一切正常」空态卡；文案更新为「把化验单拍下来交给系统读一遍」
- [x] **首页迷你趋势**：`Overview.series`（最新 3 类指标 × 近 8 点，含最新 flag）→ 首页「指标趋势」卡组
      （迷你折线 + 异常色 + 区域渐隐，点击进档案）；hero 增两个 CTA「📷 拍照化验单去解读」「🖼 发照片问医生」
- [x] **显性入口**：问答输入区「🖼 图片问诊（多选）」「📷 拍照（调用摄像头）」两个按钮；
      报告页「📷 拍照化验单」按钮 + 仓库示例图指引；`?photo=1` / `?camera=1` 入口带引导提示
      （浏览器不允许无手势自动拉起文件选择器，故为提示而非自动弹窗）
- [x] **演示数据播种**：`scripts/seed-demo-usage.py` 对 user 账号播种
      （问答×2+收藏、科室导诊、档案+指标+病史+AI 建议、报告上传、admin 知识库补录）；
      另补 4 条历史指标点（8-08/8-12）使首页趋势 ≥3 点 —— 新账号登录即有内容，不再"一排零"

### J. 前端视觉：报告单 / 动效 / 深色模式 / 空态 ✅

- [x] ReportDetail 化验单版式：网格化项目/结果/参考范围/标志/解读五列，异常行整行着色 + 左侧色条，
      窄屏降级为两段式；替代原 el-table
- [x] 流式打字光标：MessageList 最后一条回答输出中显示闪烁 `▍`（`blink` 动画，仅 streaming 时出现）
- [x] 深色模式：`utils/theme.ts`（html.dark + localStorage + prefers-color-scheme 自适应），
      `styles/index.css` 暗色 token 全量覆盖（含 Element Plus 变量、表格、滚动条、免责声明）；
      顶栏一键切换；ECharts（MetricTrendSection / Vectors）监听 `theme-change` 重建配色
- [x] 空态/文案统一：报告上传页提示改为「拍一张化验单」；首页「一切正常」空态卡；
      原有 empty-sheet 体系维持

### K. 报告 PDF 导出 ✅

- [x] ReportDetail「导出 PDF」：html2canvas 截图 + jsPDF 生成 A4 多页 PDF，
      仅导出「指标明细 + 总体解读」，文件名 `康识-<报告名>-<日期>.pdf`；失败提示改用浏览器打印
- [x] 体积优化（黑盒实测）：scale 2+PNG → **scale 1.5 + JPEG(0.92)**，同一报告 20MB → **664KB**
- [x] 依赖新增：`jspdf` / `html2canvas`（npm audit 命中的为既有 echarts/vite 链漏洞，与两者无关，未升级）
- [x] **仓库示例图**：`samples/real-lab-cmp.jpg` —— **真实检验报告照片**（Wikimedia Commons "CMP report.JPG"，
      作者 Bobjgalindo，CC BY-SA 4.0；含 GLUCOSE/BUN/ALT 等英文指标与 HI 标志）——
      无化验单照片时用它体验拍照识别与图片问诊（报告页与 README 均给出路径并署名）

### L. 验证 ✅

- [x] 后端 `mvn test` 90/90；前端 `vue-tsc --noEmit` 与 `npm run build` 通过
- [x] **真实环境黑盒冒烟**（本机 Chrome + playwright-core，前后端已启动、Redis/Milvus 在线、真实 LLM key）：
      登录（admin/Admin123!）→ 首页（「一切正常」空态卡与统计）→ 智能问答**真流式**回答 + 打字光标 →
      **化验单照片上传 → 多模态识别 6/6 指标 → 报告单视觉（异常行整行标红）→ 真实 LLM 总体解读** →
      **导出 PDF（664KB 落盘）** → 深色模式一键切换（html.dark）→ 管理员向量页（Milvus HNSW / 88 条）。
      **全程控制台 0 错误、0 失败请求**；证据截图与脚本见 `.smoke-evidence/`（`node .smoke-evidence/smoke.mjs` 可复跑）
- [x] **用户流黑盒冒烟**（播种数据登录 user/User123!，全程真实化验单照片）：首页「需要留心」6 张 + 迷你趋势 3 张 →
      **图片问诊**（上传 `samples/real-lab-cmp.jpg` → 追问"哪些指标偏高" → 回答逐项解读 HI 指标）→
      报告页拍照化验单入口 → **真实照片识别渲染 10 行指标**（空腹血糖 9.1 mmol/L 偏高、谷丙转氨酶 47 U/L 偏高）→
      深色模式 → 管理员向量页。**11/11 全 PASS、控制台 0 错误**；`smoke-user.mjs` 可复跑
- [x] 文档同步：《项目改进纪要.md》脚本重写为「就医之旅」故事线（含演示前检查清单第 7 步：准备化验单照片）；
      README 更新拍照识别/暗色模式/PDF 导出与测试数；本清单同步

---

## 阶段⓪ 前轮已修复的真缺陷（回填：《改进纪要》5.2 记录了、本清单此前漏记）

> 以下五项在代码中均已修复（复检确认），并持续有测试或文档覆盖。为清单完整性回填于此。

- [x] **schema.sql 建表顺序错误（最严重）** — 原 `ALTER TABLE` 排在 `CREATE TABLE` 之前、
      被 `continue-on-error` 静默吞掉，全新环境首次部署缺字段。
      现 `db/schema.sql` 重写为**纯幂等 `CREATE TABLE IF NOT EXISTS`**（无 ALTER，历史变更并入表定义）。
- [x] **SSE 问答在真实 Tomcat 上崩溃** — Sa-Token 上下文过滤器默认只注册 `REQUEST` 分发类型，
      流式结束后的 `ASYNC` 再分发拿不到上下文直接抛异常。
      现 `SaTokenConfig` 显式注册全部 `DispatcherType`（`EnumSet.allOf`）。
- [x] **体检指标重复写入** — 上传选档案导入、详情页「写入档案」两条路径都无幂等，会写重。
      现 `HealthService.importReportItems` 以 (profile, metricType) 幂等比对：存量预载 + 循环内内存判定，
      已存在则更新而非新插（`ReportImportIdempotencyTest` 覆盖两条路径复验）。
- [x] **模型名已下线** — 原默认模型名 DeepSeek 已停用，收到 400 后静默回退模板回答，
      表面「能用」实则大模型一直没接上。现默认 `deepseek-v4-flash-vision-exp`
      （`.env` / README / application.yml 同步）；`LlmClient` 失败仍回退模板并 log.warn 留痕。
- [x] **SSE 连接泄漏** — 客户端中断时未关闭上游流，HTTP 连接挂到超时。
      现 `LlmClient.streamRemote` 以 try-with-resources 关闭 token 流取消上游订阅；
      前端 `sse.ts` 内部 AbortController 与外部 signal 联动、`useChatStream` 卸载时主动断流
      （见一轮 3a/3b，两者互为两端保障）。

---

## 第一轮执行（前一轮四阶段，均已完成）

### 阶段① 正确性

- [x] **1a. RoutingVectorStore 补账队列** — Milvus 故障窗口内的写入（upsert / 按文档删除）
      记入有界 FIFO（上限 5000，超出丢最旧并告警），Milvus 恢复可达时在
      search/upsert/size/loadAll 入口自动按序补写；删除以墓碑参与排序，
      直达删除成功会清掉队列中同文档的旧插入避免复活；`info()` 显示「待补写 N 条」。
      文件：`rag/RoutingVectorStore.java`
- [x] **1b. 启动跳过无谓重建** — `RagService.rebuildIfStale(force)`：DB chunk 数 = 向量库条数
      时不清空重建，经新增的 `VectorStore.loadAll()` 把已算好的向量回灌内存备份
      （Milvus 用 query 全量读回，内存库直接导出）；对不上或回灌行数短缺才走全量重建。
      新配置 `APP_KB_FORCE_REINDEX`（application.yml / .env.example 已同步）。
      文件：`rag/StoredChunk.java`(新)、`rag/VectorStore.java`、`rag/MilvusVectorStore.java`、
      `rag/InMemoryVectorStore.java`、`rag/RagService.java`、`bootstrap/DataSeeder.java`

### 阶段② 安全

- [x] **2a. 登录/注册限流** — RateLimiter 增加 String subject 重载（原 Long 版委托），
      新增 `common/ClientIp.java`（X-Forwarded-For 首值优先）；登录与注册各自独立按 IP 限
      默认 10 次/分。测试环境默认关（limit=0），专门的 AuthApiRateLimitTest 压到 2 验证 429。
- [x] **2b. 上传魔数校验** — 新增 `common/FileMagic.java`（png/jpeg/gif87a/89a/webp/pdf/
      zip(docx)/ole2(doc)/文本签名；文本判定 = 无 NUL 且 UTF-8 可无损解码）。
      DocumentParser.validateUpload 与 ChatImageService.upload 均做内容↔扩展名一致性校验；
      图片 MIME 改由检出类型推导，不再信任客户端 Content-Type。

### 阶段③ 体验：SSE 可中断

- [x] **3a. sse.ts** — 内部 AbortController 与外部 signal 联动；75 秒空闲看门狗超时报
      「回答超时」；非 2xx 解析响应体业务 message（429 有专门文案）；
      导出 `StreamAborted` 供调用方区分「主动停止」。
- [x] **3b. Chat.vue** — send 创建 AbortController；流式期间按钮变「停止」；中止保留部分回答
      （空则落「已停止生成。」）；`onBeforeUnmount` 中止残留连接；addFiles 补单张 >4MB 预检。

### 阶段④ 测试护底

- [x] 4a. `rag/RoutingVectorStoreTest` — 降级入队+内存可检索、恢复按序重放（含中途失败续传）、
      删除墓碑清旧插入、队列封顶不炸，共 4 例
- [x] 4b. `rag/RebuildDecisionTest` — 新鲜→零 embedding 回灌、数量不符→全量、loadAll 短缺→全量、
      force 绕过判定，共 4 例
- [x] 4c. `service/ReportParserTest` — 血糖超标/血压双值/自带区间判低/噪声行跳过/空输入/
      interpretItem 四分支，共 6 例（该类此前零测试）
- [x] 4d. `common/FileMagicTest` — 全签名识别、二进制判否、扩展名映射、伪装样本 400 拒绝 + 真 PNG 通过
- [x] 4e. `AuthApiRateLimitTest` — 限额压到 2，第 3 次登录 429 且 code/message 正确
- [x] 4f. 全量回归：后端 62/62 通过；前端 `vue-tsc --noEmit` 与 `npm run build` 通过

### 落地过程中的额外修复

- ReportParser 的 `skipName` 增加「以『号』结尾不算指标」规则 —— 测试发现
  「样本号：123456789」这类标识行会被解析成 unknown 指标（中文指标名不以号结尾）。

---

## 二、后备池（未排期，按价值排序）

### 一致性 / 事务（改动面大，单独排期）

| 项 | 位置 | 说明 |
|---|---|---|
| @Transactional 内同步调 LLM | ReportService.upload | 事务+连接池在数十秒外呼期间空占 |
| 入库循环逐条 insert+逐条远程 embedding 在事务内 | KnowledgeService.persistDocument | 大文档占死 DB 连接 |
| ask() 四次写无事务 | ChatService.ask / bindToMessage | 中途异常留孤儿消息 |
| deleteProfile/deleteSession 多表删无事务 | HealthService / ChatService | 中途失败出现孤儿行/文件 |

### 剩余小项

| 项 | 位置 | 说明 |
|---|---|---|
| 缺 ESLint/Prettier/editorconfig/git hooks | frontend 根 | 已决策暂缓；死代码清扫目前靠 noUnusedLocals |
| 无任何前端测试框架 | package.json | 关键 utils 先补 vitest |
| token 存 localStorage 且散读 | stores/user.ts 等 | XSS 可窃取；中期 httpOnly cookie |
| 启动 rebuild 一次性全量载入内存再串行建索引 | RagService.rebuildFromDatabase | 低优先 |
| 运行期 Redis 掉线不回切内存 | CacheService degrade | 恢复探测低优先 |
| schema continue-on-error 静默吞错 | application.yml:57 | 至少启动时打印被吞的错误 |
| 知识库表格时间列显示原始 ISO 串 | admin/Knowledge.vue createdAt 列 | 既有行为非回归，可改 formatWhen |
| Vectors/首页零值空态打磨等体验项 | 若干 | 低优先 |

---

## 三、已知接受的取舍（本轮不处理）

- rebuildIfStale 的「数量一致」判据无法发现**内容改了但条数没变**的情况——
  后台手动 reindex 或 `APP_KB_FORCE_REINDEX=true` 兜底。
- 限流窗口固定默认 60s（env 可整体调整），暂不做滑动窗口。
- 分页接口第 1 版只覆盖四个增长最快的列表；档案指标/病史列表数据量天然小，维持全量。
- 前端停止生成依赖 AbortController 客户端断开；后端 emitter 检测到断连会自然终止 LLM 流，
  服务端侧已产生的 token 计费不受影响。
- SSE 有界线程池满时明确拒绝（429）而非无限排队 —— 并发演示场景远够。
